spring:
  application:
    name: UserService
  datasource:
    url: jdbc:mysql://localhost:3306/sagapattern
    username: root
    password: Upen@1988
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


  cloud:
    stream:
      function:
        definition: payementProcessor
      bindings:
        payementProcessor-in-0:
          destination: order-event
        payementProcessor-out-0:
          destination: payment-event

